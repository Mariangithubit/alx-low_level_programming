#include "operation.h"

/**
 * add - add calc
 * @a: first integer
 * @b: second integer
 * Return: claculation of 2 numbers
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub - subtraction calc
 * @a: first integer
 * @b: second integer
 * Return: claculation of 2 numbers
 */
int sub(int a, int b)
{
        return (a - b);
}

/**
 * mul - multiply calc
 * @a: first integer
 * @b: second integer
 * Return: claculation of 2 numbers
 */
int mul(int a, int b)
{       
        return (a * b);
}

/**
 * div - division calc
 * @a: first integer
 * @b: second integer
 * Return: claculation of 2 numbers
 */
int div(int a, int b)
{       
        return (a / b);
}

/**
 * mod - module calc
 * @a: first integer
 * @b: second integer
 * Return: claculation of 2 numbers
 */
int mod(int a, int b)
{       
        return (a % b);
}
